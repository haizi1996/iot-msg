## HBase 配置
spring:
  data:
    hbase:
      config:
        hbase:
          zookeeper:
            quorum: centosBase
          rootdir: file:///workspace/hbase
          fs:
            default:
              name: file:///workspace/hbase
